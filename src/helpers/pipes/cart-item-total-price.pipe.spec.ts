import { CartItemTotalPricePipe } from './cart-item-total-price.pipe';

describe('CartItemTotalPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CartItemTotalPricePipe();
    expect(pipe).toBeTruthy();
  });
});

import { HighLightBorderDirective } from './high-light-border.directive';

describe('HighLightBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightBorderDirective();
    expect(directive).toBeTruthy();
  });
});
